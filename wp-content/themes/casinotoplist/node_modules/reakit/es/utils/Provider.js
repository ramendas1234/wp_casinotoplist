import { warning } from 'reakit-utils/warning';
import 'react';
import 'reakit-system/SystemProvider';
import '../Id/IdProvider.js';
export { Provider } from '../Provider.js';

process.env.NODE_ENV !== "production" ? warning(true, "[reakit/Provider]", "Importing `Provider` from `reakit/utils` or `reakit/utils/Provider` is deprecated.", "Please, import `Provider` from `reakit` or `reakit/Provider` instead") : void 0;
